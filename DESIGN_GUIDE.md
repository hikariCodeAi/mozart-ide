# Mozart IDE 官网设计规范

本规范约束官网的视觉实现。当前主方向为品牌型产品官网：真实产品截图优先，深海军蓝与石墨色承担结构，青色表达交互状态，古金色只用于连接 Mozart 品牌气质的少量强调。

## 字体

- 标题：`--font-heading`，使用 Iowan Old Style、宋体类系统字体及 Georgia 回退。
- 正文与按钮：`--font-body`，使用系统无衬线字体，并包含苹方、思源黑体和微软雅黑等中文回退。
- 标签：`--font-label`，继承正文体系，通过字重和字距建立层级。
- 代码、序号与时长：`--font-code`，使用系统等宽字体。
- 不加载远程字体；字体降级不得引发布局跳动。

## 颜色

- 页面背景：`--page-bg`。
- 面板背景：`--panel-bg`、`--panel-bg-raised`。
- 主文本与弱文本：`--text`、`--muted-text`。
- 品牌色与品牌暖色：`--brand`、`--brand-warm`。
- 普通边框与键盘焦点：`--border`、`--focus`。
- 状态色：`--success`、`--warning`、`--danger`、`--info`。
- 青色仅表示当前项、可操作反馈或焦点；古金色只作细线、节点和品牌氛围，不用于大面积按钮。

## 间距与圆角

- 以 `--space-unit: 0.25rem` 为基础，优先使用其 2、3、4、6、8 倍。
- 页面横向边距使用现有响应式容器规则；桌面卡片内距使用 `--panel-padding`。
- 同组控件间距使用 `--control-gap`，标准控件高度使用 `--control-height`。
- 卡片采用 `--radius-panel`，按钮采用 `--radius-control`；避免圆角、胶囊和直角在同一控件组中无规则混用。
- 移动端压缩横向间距并隐藏次要按钮文字，但保留至少 40px 的点击区域。

## 布局

- 普通组件默认使用 Flex；只有需要严格二维对齐的英雄区和五项页签使用 Grid。
- 轮播截图始终保持 `171 / 103`，使用真实截图与 `object-fit: contain`。
- 五组能力上下切换，每组内的截图左右切换；桌面端使用与截图相连的垂直指挥栏，小于 1100px 后切换为截图下方的横向控制栏，避免压缩截图内容。
- 桌面端完整首屏不超过 `100svh`；小于 1100px 后改为纵向排列并允许自然滚动。
- 截图、轮播控件和文字预留稳定高度，切换语言与幻灯片时不得跳动。
- 页码固定在截图容器右上方，五组能力使用同一顶部偏移。

## 图标

- 品牌 SVG 与彩色 PNG 统一放在 `assets/brand/`；真实产品截图放在 `assets/screenshots/`。
- 简单单色控制图标使用内联 SVG、`currentColor` 和统一描边；装饰图标设置 `aria-hidden="true"`。
- 可交互图标必须位于原生按钮或链接内并提供可访问名称；不得使用可点击 `div`。
- 同一图标不得复制为多份素材文件。

## 按钮

- 页面按钮复用现有适配类：`header-cta`、`primary-action`、`secondary-action`、`arrow`、`pause-button` 和 `tab`。
- 按钮必须有原生类型、hover、focus、disabled 状态；图标跟随文字颜色。
- 轮播箭头、暂停和页签必须保持键盘可访问，并使用翻译对象中的可访问名称。

## 动效

- `--motion-fast` 用于按钮反馈，`--motion-normal` 用于局部状态，`--motion-slow` 用于幻灯片切换。
- 只为状态变化、层级和反馈添加动效；不添加纯装饰的循环界面动画。
- 能力组自动播放周期为 8 秒，每组内截图每 4 秒切换。悬停、焦点、页面隐藏、显式暂停时两层自动播放均停止。
- `prefers-reduced-motion: reduce` 下关闭自动播放和非必要过渡，状态变化仍需清楚可见。
