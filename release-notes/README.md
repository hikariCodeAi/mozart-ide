# Release notes

Add one release-note file for each verified public GitHub Release. The private publishing pipeline may copy generated notes into this directory, but this public repository intentionally contains no private publishing script, server credentials, or signing secrets.

Do not publish placeholder versions, checksums, installer URLs, or claims that have not been verified against the corresponding GitHub Release.
