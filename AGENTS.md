# Mozart IDE 官网协作约定

本文件适用于整个仓库。这里是 Mozart IDE 的公开官网与发布元数据仓库，不是私有产品源码仓库；父级目录中面向 C++ 产品工程、服务器、PM2 或 Prisma 的规则不适用于本仓库。

## 开始前

- 先完整阅读根目录 `README.md`。
- 修改素材前阅读 `assets/ASSET_MANIFEST.md`；修改首页布局、文案或交互前阅读对应的 `docs/` 说明文件（若存在）。
- 改代码前优先阅读当前目录中的 `README.md`、`README.txt`、`readme.md` 或 `readme.txt`。
- 只修改当前任务涉及的文件，保留用户已有的未提交文件和改动。

## 实现原则

- 新增或调整视觉样式前必须遵循根目录 `DESIGN_GUIDE.md` 中的设计变量、按钮、图标和动效规则。
- 代码尽量精简，优先复用现有结构、状态和样式，保持高内聚、低耦合，避免无需求的抽象或依赖。
- 页面保持静态架构：`index.html` 负责语义结构，`styles.css` 负责视觉与响应式，`app.js` 负责文案、状态和交互。
- 当前运行时为通过 CDN 固定版本引入的 Vue 2 与 Tailwind CSS Browser；未经明确需求，不引入打包器、Node 运行时、第三方轮播库、远程字体或新的前端框架。
- 标题、正文、按钮、页签、箭头、进度和双语文案必须由 HTML/CSS 渲染，不得把整张设计稿当网页背景。
- 只使用真实 Mozart IDE 截图和已登记素材；不得生成虚假 IDE 界面、伪造性能数字或暴露令牌、用户名、私有路径。
- 保持截图容器 `171:103`，桌面完整首屏不超过 `100svh`，移动端允许纵向排列且不得横向溢出。
- 单个手写源码或测试文件最多 2000 行。功能变更同步更新相关 README、素材清单或 `docs/` 文档；目录结构以实际仓库为准。

## 发布真实性

- `update/latest.json` 是下载状态的唯一事实来源。
- 只有经过验证的 GitHub Release 和真实安装包 URL 同时存在时，才可设置 `published: true`。
- 未发布、清单不可用或请求失败时必须保持“即将发布 / Coming soon”，不得添加占位下载链接。
- 保留 `update/`、`release-notes/` 和 `.nojekyll`；不要把本地设计探索稿、测试报告或凭据加入 Pages 发布内容。

## 验证要求

- 至少检查 `1920×1080`、`1440×900`、`1280×720`、`768×1024`、`390×844`。
- 验证中英文即时切换及持久化、四个轮播状态、页签、箭头、键盘、触摸、暂停、8 秒自动播放和 `prefers-reduced-motion`。
- 验证所有图片成功加载、桌面无纵向滚动、移动端无横向溢出、文案和截图切换无布局跳动。
- JavaScript 改动至少执行 `node --check app.js`，JSON 改动至少执行 `python3 -m json.tool update/latest.json`，提交前执行 `git diff --check`。

## 部署边界

- 推送到 GitHub `main` 后由 `.github/workflows/pages.yml` 自动部署 GitHub Pages。
- 每次完成用户要求的代码、文案或素材修改并通过相关验证后，默认只提交本次任务涉及的文件并推送到 `origin/main`，无需再次询问；不得顺带提交用户已有的其他未提交或未跟踪内容。推送失败时保留本地提交并明确报告原因。
- 本仓库没有自管服务器、SSH 密码、`post-receive`、PM2、Prisma 或数据库迁移流程；不要为这些不存在的设施添加模板、占位配置或秘密信息。
